<input type="checkbox" id="menuToggle">

<div id="topNav">
  <label for="menuToggle" class="menu-icon">&#9776;</label>
  <img id="brandImg" draggable="false" src="/assets/attender.svg"/ />
</div>

<nav class="menu">
      <a href="/profile"><i class="fa fa-user" aria-hidden="true">&#160;</i>Profile</a>
      <a href="/"><i class="fa fa-book" aria-hidden="true">&#160;</i> Course</a>
      <% if current_student %>
      <%= link_to(destroy_student_session_path, :method => :delete) do %>

      <i class="fa fa-sign-out" aria-hidden="true">&#160;</i>Log Out
      <% end %>
      <% else %>
      <%= link_to(destroy_teacher_session_path, :method => :delete) do %>

      <i class="fa fa-sign-out" aria-hidden="true">&#160;</i>Log Out
      <% end %>
      <% end %>

</nav>


<% if current_teacher.present? %>

  <h1>Teacher : <%= current_teacher.first_name %> <%= current_teacher.last_name %></h1>

  <%= link_to "Create a new Course", new_course_path %>
  <br>
  <br>
  <% current_teacher.courses.each do |course| %>
     <div class="course">
      <%= link_to "Course: #{course.name}", course_path(course) %>
      <br>
      <%= link_to "Edit Course", edit_course_path(course) %>
      <br>

      <%= link_to "Delete Course", course, method: :delete, data: {confirm: "Are you sure?"} %>

    </div>
    <br>
  <% end %>
<% end %>

<% if current_student %>
  <div class="enrolled-courses">
    <h1 class="student-name">Student: <%= current_student.first_name %> <%= current_student.last_name %></h1>
    <%= hidden_field_tag(:student_id, current_student.id) %>

    <div class="course-search">
      <%= form_tag do %>
        <%= text_field_tag :course_name, 'search course'%>
        <div class="results"></div>
        <div class="btn"></div>
      <% end %>
    </div>
  </div>

  <div class="my-courses">
    <div class="my-courses-heading"><h2>My courses</h2></div>
      <center>
      <div class="enrolled-courses">
        <% current_student.courses.each do |enrolled_course| %>
          <div class="enrolled-course" data-course-id=<%= enrolled_course.id %>>
            <%= link_to "#{enrolled_course.name}", course_path(enrolled_course), class: "enrolled" %>
            <p class="teacher"> <%= enrolled_course.teacher.first_name %> <%= enrolled_course.teacher.last_name%></p>
          </div>
        <% end %>
      </div>
      </center>
  </div>
<% end %>
